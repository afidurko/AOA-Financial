{
  "install": "pip install -e \".[dev,web]\"\nif command -v npm >/dev/null 2>&1; then (cd web-mobile && npm ci && npm run build); fi\npython3 -m aoa.cli attl init\n./scripts/write-aoa-workspace.sh"
}
